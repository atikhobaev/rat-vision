# 🤝 Contributing

1. Fork the repository and create a focused branch.
2. Keep RAT VISION's display-output-only boundary intact.
3. Add a failing test before behavior changes.
4. Run `python -m pytest -q` and `python -m compileall -q ratvision`.
5. Open a pull request describing Windows/GPU/monitor hardware used for manual testing where relevant.
